runtime: java
env: flex

env_variables:
  org.apache.cxf.jaxws.checkPublishEndpointPermission: 'false'

handlers:
- url: /.*
  script: this field is required, but ignored

runtime_config:
   jdk: openjdk8
   server: jetty9